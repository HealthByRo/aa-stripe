language: python
matrix:
    include:
        - python: 3.6
          dist: xenial
          sudo: false
        - python: 3.7
          dist: xenial
          sudo: true
        - python: 3.8
          dist: xenial
          sudo: true
        - python: 3.9
          dist: xenial
          sudo: true

install:
  - pip install tox tox-travis coverage coveralls

script:
  - tox

after_success:
  - coverage report -m
  - coveralls
